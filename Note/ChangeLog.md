# Change Log

本文件记录本地为复现、整理和提交课程项目所做的改动。

## 2026-05-19

### Added

- Created `Note/` directory for reproduction notes.
- Added `Note/RunLog.md` to record environment setup, data preparation, commands, generated outputs, and reproduction status.
- Added `Note/ChangeLog.md` to track local documentation and code changes.
- Added `Note/CodeSummary.md` to summarize repository structure and core code flow.
- Added `Note/results/` for reproduced experiment outputs.
- Added reproduced result JSON files and run logs under `Note/results/`.
- Added `Note/results/pip_freeze.txt` to record dependency versions in the conda environment.

### Notes

- The repository was cloned through HTTPS because SSH access failed with `Permission denied (publickey)`.
- A conda environment named `ml_project_repro` was created for reproduction.
- No source code logic was changed.

## Future Changes

| Date | Type | File / Module | Description |
|------|------|---------------|-------------|
| TBD | TBD | TBD | Record future experiment reproduction changes here |
